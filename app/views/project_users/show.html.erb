<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to 'Project Users', project_project_users_path(@project) %> > Project User #<%= @project_user.id %></h1>
      <%= link_to 'Edit', edit_project_project_user_path(@project, @project_user), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Project</h5>
        <%= @project_user.project_id %>
      </div>
            <div class="mb-4">
        <h5>Account</h5>
        <%= @project_user.account_id %>
      </div>
          </div>
  </div>
</div>
