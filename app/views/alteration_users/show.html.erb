<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to 'Alteration Users', alteration_alteration_users_path(@alteration) %> > Alteration User #<%= @alteration_user.id %></h1>
      <%= link_to 'Edit', edit_alteration_alteration_user_path(@alteration, @alteration_user), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Account</h5>
        <%= @alteration_user.account_id %>
      </div>
            <div class="mb-4">
        <h5>Alteration</h5>
        <%= @alteration_user.alteration_id %>
      </div>
          </div>
  </div>
</div>
