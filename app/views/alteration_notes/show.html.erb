<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to 'Alteration Notes', alteration_notes_path %> > Alteration Note #<%= @alteration_note.id %></h1>
      <%= link_to 'Edit', edit_alteration_note_path(@alteration_note), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Alteration</h5>
        <%= @alteration_note.alteration %>
      </div>
            <div class="mb-4">
        <h5>User</h5>
        <%= @alteration_note.user %>
      </div>
          </div>
  </div>
</div>
