<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to 'Cooling Types', cooling_types_path %> > Cooling Type #<%= @cooling_type.id %></h1>
      <%= link_to 'Edit', edit_cooling_type_path(@cooling_type), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Name</h5>
        <%= @cooling_type.name %>
      </div>
          </div>
  </div>
</div>
