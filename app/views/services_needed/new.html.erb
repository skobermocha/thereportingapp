<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3">New Services Needed</h1>
      <%= link_to 'Cancel', alteration_system_services_needed_path(@alteration, @system), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
      <%= render 'form', services_needed: @services_needed %>
    </div>
  </div>
</div>
